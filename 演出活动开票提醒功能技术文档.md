# 演出活动开票提醒功能技术文档

## 📋 功能描述

在演出活动开票前的特定时间点（**1天前、1小时前、5分钟前**）自动发送提醒消息，帮助用户及时获知开票时间，提升用户体验。

---

## 🎯 业务场景

### 用户痛点
- 演唱会开票时间难记，容易错过
- 热门演出需要提前准备，临时得知来不及
- 希望在开票前收到提醒通知

### 解决方案
- **开票前1天提醒**：提前通知用户，有充足时间准备
- **开票前1小时提醒**：临近提醒，确保用户记得
- **开票前5分钟提醒**：最后提醒，用户可立即进入抢票页面

---

## 🏗️ 技术架构

### 核心组件

```
┌─────────────────────────────────────────────────────────────┐
│            ShowEventReminderScheduler (定时扫描器)           │
│                   每分钟扫描一次                              │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      ↓
         查询即将开票的演出活动（25小时内）
                      │
                      ↓
         ┌────────────┴────────────┐
         │  计算距离开票时间差      │
         │  检查是否到达提醒时间点  │
         └────────────┬────────────┘
                      │
         ┌────────────┴────────────┐
         │   检查 Redis 是否已发送  │
         │   避免重复提醒          │
         └────────────┬────────────┘
                      │
                      ↓ 未发送
         ┌────────────┴────────────┐
         │   发送提醒到 Kafka       │
         │   记录发送状态到 Redis   │
         └────────────┬────────────┘
                      │
                      ↓
         ┌────────────┴────────────────────────┐
         │        Kafka Consumer (消费者)       │
         │  - 发送短信/推送通知                 │
         │  - 发送邮件通知                      │
         │  - 记录通知历史                      │
         └──────────────────────────────────────┘
```

---

## 📁 文件清单

### 新增文件

1. **ShowEventReminderEvent.java**
   - 路径：`com.example.yoyo_data.common.dto.ShowEventReminderEvent`
   - 作用：演出提醒事件 DTO，包含演出信息、提醒类型、提醒时间等

2. **ShowEventReminderScheduler.java**
   - 路径：`com.example.yoyo_data.infrastructure.scheduler.ShowEventReminderScheduler`
   - 作用：定时扫描即将开票的演出活动，发送提醒消息

### 修改文件

1. **KafkaTopic.java**
   - 新增常量：
     - `SHOW_REMIND_ONE_DAY` - 开票前1天提醒主题
     - `SHOW_REMIND_ONE_HOUR` - 开票前1小时提醒主题
     - `SHOW_REMIND_FIVE_MINUTES` - 开票前5分钟提醒主题

2. **TicketRedisKey.java**
   - 新增常量：
     - `SHOW_REMIND_PREFIX` - 提醒状态记录前缀

---

## 🔧 技术细节

### 1. 定时扫描策略

**扫描频率**: 每分钟执行一次（Cron: `0 * * * * ?`）

**查询范围**: 当前时间 到 未来25小时内开票的演出

**原因**:
- 覆盖1天（24小时）提醒的时间范围，预留1小时缓冲
- 每分钟扫描一次，确保提醒及时性

```java
@Scheduled(cron = "0 * * * * ?") // 每分钟的第0秒执行
public void scanShowEventsForReminders() {
    // 查询范围：当前时间 到 未来25小时
    LocalDateTime startTime = now;
    LocalDateTime endTime = now.plusHours(25);

    // 只查询 PENDING 状态的演出
    queryWrapper.eq(ShowEvent::getStatus, ShowEventStatus.PENDING)
                .ge(ShowEvent::getSaleStartTime, startTime)
                .le(ShowEvent::getSaleStartTime, endTime);
}
```

---

### 2. 提醒时间点判断

**时间容差**: ±2分钟

**原因**: 避免因扫描间隔（1分钟）错过提醒时间点

```java
// 判断是否到达提醒时间点
long timeDifference = Math.abs(timeUntilSale - advanceTime);
long toleranceMillis = 2 * 60 * 1000L; // 2分钟容差

if (timeDifference <= toleranceMillis) {
    // 发送提醒
}
```

**示例**:
- 开票时间：2026-02-10 10:00:00
- 1小时前提醒：2026-02-10 09:00:00
- 实际扫描时间：2026-02-10 08:59:30（在容差范围内，会发送提醒）

---

### 3. 避免重复发送

**使用 Redis 记录发送状态**

**Key 格式**: `ticket:show:remind:{showEventId}:{reminderType}`

**示例**:
```
ticket:show:remind:1:1day     → "1" (表示1天前提醒已发送)
ticket:show:remind:1:1hour    → "1" (表示1小时前提醒已发送)
ticket:show:remind:1:5min     → "1" (表示5分钟前提醒已发送)
```

**过期时间**: 2天（确保不会重复发送）

```java
String reminderKey = buildReminderKey(show.getId(), reminderType);
Boolean alreadySent = redisService.exists(reminderKey);

if (Boolean.TRUE.equals(alreadySent)) {
    log.debug("【演出提醒】已发送过");
    return;
}

// 发送提醒后，标记为已发送
redisService.set(reminderKey, "1", 2 * 24 * 60 * 60L);
```

---

### 4. Kafka 消息发送

**Topic 选择**:
- 1天前提醒 → `show-remind-one-day`
- 1小时前提醒 → `show-remind-one-hour`
- 5分钟前提醒 → `show-remind-five-minutes`

**消息内容**:
```json
{
  "eventType": "show-remind-one-hour",
  "source": "ShowEventReminderScheduler",
  "data": {
    "showEventId": 1,
    "showName": "周杰伦2026世界巡回演唱会-北京站",
    "showType": "CONCERT",
    "venueName": "国家体育场（鸟巢）",
    "saleStartTime": "2026-02-10T10:00:00",
    "reminderType": "ONE_HOUR_BEFORE",
    "timeUntilSale": 3600000,
    "reminderTime": "2026-02-10T09:00:00"
  },
  "timestamp": "2026-02-10T09:00:00",
  "priority": 9
}
```

---

### 5. 提醒消息内容

**1天前提醒**:
```
【开票提醒】周杰伦2026世界巡回演唱会-北京站 将在明天 02-10 10:00 开始售票，请提前做好准备！
```

**1小时前提醒**:
```
【开票提醒】周杰伦2026世界巡回演唱会-北京站 将在1小时后 02-10 10:00 开始售票，请准备好抢票！
```

**5分钟前提醒**:
```
【开票提醒】周杰伦2026世界巡回演唱会-北京站 即将在5分钟后 02-10 10:00 开始售票，请立即进入抢票页面！
```

---

## 📊 数据流转

```
定时任务扫描
    ↓
查询数据库（tb_show_event）
    ↓
计算时间差
    ↓
检查 Redis（是否已发送）
    ↓ 未发送
发送 Kafka 消息
    ↓
标记 Redis（已发送）
    ↓
Kafka Consumer 消费
    ↓
发送短信/推送/邮件
```

---

## 🧪 测试验证

### 1. 准备测试数据

在数据库中插入测试演出活动：

```sql
-- 插入测试演出（10分钟后开票）
INSERT INTO tb_show_event
(show_name, show_type, venue_id, venue_name, show_time, sale_start_time, sale_end_time,
 total_seats, available_seats, locked_seats, sold_seats, max_buy_limit, status)
VALUES
('测试演唱会', 'CONCERT', 1, '测试场馆',
 DATE_ADD(NOW(), INTERVAL 2 HOUR),
 DATE_ADD(NOW(), INTERVAL 10 MINUTE),
 DATE_ADD(NOW(), INTERVAL 1 DAY),
 1000, 1000, 0, 0, 2, 'PENDING');
```

### 2. 清除提醒记录（用于重复测试）

```java
// 调用手动清除方法
showEventReminderScheduler.clearReminderRecords(1L);
```

### 3. 手动触发扫描

```java
// 调用手动触发方法
showEventReminderScheduler.manualTriggerScan();
```

### 4. 查看日志

**扫描日志**:
```log
【演出提醒扫描】开始扫描演出活动...
【演出提醒扫描】找到 1 个即将开票的演出活动
【演出提醒事件发送】showEventId=1, showName=测试演唱会, reminderType=5分钟前, topic=show-remind-five-minutes
【演出提醒发送成功】showEventId=1, showName=测试演唱会, reminderType=5分钟前
【演出提醒扫描】扫描完成
```

### 5. 验证 Redis 记录

```bash
# 连接 Redis
redis-cli

# 查看提醒记录
KEYS ticket:show:remind:*

# 输出示例
1) "ticket:show:remind:1:5min"
2) "ticket:show:remind:1:1hour"
3) "ticket:show:remind:1:1day"

# 查看具体值
GET ticket:show:remind:1:5min
# 输出: "1"
```

### 6. 验证 Kafka 消息

**查看 Kafka Topic**:
```bash
# 列出所有 Topic
kafka-topics.sh --list --bootstrap-server localhost:9092

# 消费提醒消息
kafka-console-consumer.sh \
  --bootstrap-server localhost:9092 \
  --topic show-remind-five-minutes \
  --from-beginning
```

---

## 🎨 消费者实现示例

### 创建提醒消费者（可选）

```java
@Slf4j
@Component
public class ShowEventReminderConsumer {

    @KafkaListener(topics = "show-remind-five-minutes", groupId = "show-remind-consumer-group")
    public void handleFiveMinutesReminder(String message) {
        log.info("【5分钟提醒】收到消息: {}", message);

        MessageEvent event = JSON.parseObject(message, MessageEvent.class);
        ShowEventReminderEvent reminder = JSON.parseObject(event.getData(), ShowEventReminderEvent.class);

        // 发送短信通知
        sendSMS(reminder);

        // 发送 App 推送
        sendPushNotification(reminder);

        // 记录通知历史
        saveNotificationHistory(reminder);
    }

    @KafkaListener(topics = "show-remind-one-hour", groupId = "show-remind-consumer-group")
    public void handleOneHourReminder(String message) {
        // 处理1小时前提醒
    }

    @KafkaListener(topics = "show-remind-one-day", groupId = "show-remind-consumer-group")
    public void handleOneDayReminder(String message) {
        // 处理1天前提醒
    }
}
```

---

## ⚙️ 配置说明

### 扫描频率调整

如需修改扫描频率，编辑 `ShowEventReminderScheduler.java`:

```java
// 当前：每分钟执行一次
@Scheduled(cron = "0 * * * * ?")

// 修改为：每30秒执行一次
@Scheduled(cron = "0/30 * * * * ?")

// 修改为：每5分钟执行一次
@Scheduled(cron = "0 */5 * * * ?")
```

### 时间容差调整

如需修改时间容差，编辑 `ShowEventReminderScheduler.java`:

```java
// 当前：±2分钟容差
long toleranceMillis = 2 * 60 * 1000L;

// 修改为：±5分钟容差
long toleranceMillis = 5 * 60 * 1000L;
```

---

## 📈 性能优化

### 1. 数据库索引

确保以下字段有索引：
```sql
ALTER TABLE tb_show_event ADD INDEX idx_status_sale_start (status, sale_start_time);
```

### 2. 查询优化

只查询必要的字段，减少数据传输量：
```java
queryWrapper.select(
    ShowEvent::getId,
    ShowEvent::getShowName,
    ShowEvent::getSaleStartTime
    // ... 其他必要字段
);
```

### 3. Redis 缓存

可以将演出活动信息缓存到 Redis，减少数据库查询：
```java
String cacheKey = "show:event:" + showEventId;
ShowEvent show = redisService.get(cacheKey);
if (show == null) {
    show = showEventMapper.selectById(showEventId);
    redisService.set(cacheKey, show, 3600L);
}
```

---

## ⚠️ 注意事项

### 1. 时区问题

确保服务器、数据库、应用时区一致：
```java
// 使用系统默认时区
LocalDateTime now = LocalDateTime.now();

// 或指定时区
LocalDateTime now = LocalDateTime.now(ZoneId.of("Asia/Shanghai"));
```

### 2. 演出状态管理

提醒功能只针对 `PENDING` 状态的演出，开票后应及时更新状态：
```sql
-- 开票时更新状态
UPDATE tb_show_event
SET status = 'SELLING'
WHERE id = 1 AND sale_start_time <= NOW();
```

### 3. 消息幂等性

Kafka Consumer 应实现幂等性处理，避免重复通知用户：
```java
// 检查是否已发送过通知
String notificationKey = "notification:" + userId + ":" + showEventId + ":" + reminderType;
if (redisService.exists(notificationKey)) {
    return; // 已发送过，跳过
}

// 发送通知
sendNotification();

// 标记已发送
redisService.set(notificationKey, "1", 7 * 24 * 60 * 60L);
```

### 4. 定时任务重复执行

在分布式环境下，使用分布式锁避免多个实例重复扫描：
```java
String lockKey = "lock:show:remind:scan";
boolean locked = redisService.setIfAbsent(lockKey, "1", 60L);

if (!locked) {
    log.debug("其他实例正在执行扫描，跳过");
    return;
}

try {
    // 执行扫描
    scanShowEventsForReminders();
} finally {
    redisService.delete(lockKey);
}
```

---

## 📞 联系方式

- **开发团队**: YoYo Data Team
- **更新日期**: 2026-02-09
- **版本**: v1.0

---

## 📚 相关文档

- [订单创建异步化改造技术文档.md](./订单创建异步化改造技术文档.md)
- [多观影人抢票功能升级说明.md](./多观影人抢票功能升级说明.md)
